server:
  port: 8761

spring:
  application:
    name: mi-service-registry

mi:
  serviceRegistry:
    username: serviceregistry
    password: vento123
  weightService:
    username: mimyinvoiceback
    password: vento123
#  userService:
#    username: wtuserservice
#    password: vento1234

eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false
    service-url:
      defaultZone: 'http://${mi.serviceRegistry.username}:${mi.serviceRegistry.password}@localhost:8761/eureka'
  instance:
    hostname: localhost

